# Documentation build only. Kept separate from the repository-root
# requirements.txt so simulation and CI environments do not pull Sphinx.
#
#   pip install -r docs/requirements.txt      (or: make docs, which does it)
Sphinx==9.1.0
myst-parser==5.1.0
furo==2025.12.19
sphinx-copybutton==0.5.2
sphinx-autobuild==2025.8.25
