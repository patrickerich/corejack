# Shared riscv-dbg target configuration for the CoreJack FPGA targets.
# Adapter-specific wrappers (openocd-tigard.cfg, openocd-olimex-arm-usb-tiny.cfg)
# select the JTAG probe and then source this file. Boards pick their wrapper
# through the `debug.openocd_cfg` field of cfg/boards/<board>.yaml.

reset_config none

set _CHIPNAME riscv
set _EXPECTED_ID 0x00000001

jtag newtap $_CHIPNAME cpu -irlen 5 -expected-id $_EXPECTED_ID -ignore-version
set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME riscv -chain-position $_TARGETNAME -coreid 0

adapter speed 1000
riscv set_command_timeout_sec 120
riscv set_ir idcode 0x01
riscv set_ir dtmcs 0x10
riscv set_ir dmi   0x11
riscv set_mem_access progbuf abstract sysbus
gdb report_data_abort enable
gdb report_register_access_error enable
gdb breakpoint_override hard

init
